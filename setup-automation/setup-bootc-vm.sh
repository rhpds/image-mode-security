#!/bin/bash

echo "Bootc VM will be deployed automatically when image is built" > /tmp/progress.log
chmod 666 /tmp/progress.log

# This VM will be deployed via runtime automation after the student builds the security image
